@page
@model RazorSport.Pages.Atleten.IndexModel

@{
    // Layout = "_SubLayout";
    ViewData["Title"] = "Index";
}

<h1>Oh yeah lekker kaasje</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Atleet[0].Naam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Atleet[0].Geboortedatum)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Atleet[0].Nationaliteit)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Atleet) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Naam)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Geboortedatum)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nationaliteit)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
